diff --git a/cast/common/BUILD.gn b/cast/common/BUILD.gn
index b1784186..e80307c3 100644
--- a/cast/common/BUILD.gn
+++ b/cast/common/BUILD.gn
@@ -11,7 +11,9 @@ visibility = [ "./*" ]
 
 config("certificate_config") {
   visibility += [
+     "../standalone_receiver:cast_receiver_mylib",
     "../standalone_receiver:cast_receiver",
+    "../standalone_sender:cast_sender_mylib",
     "../standalone_sender:cast_sender",
   ]
   defines = []
@@ -54,7 +56,9 @@ openscreen_source_set("certificate_boringssl") {
   # use [ "*" ] here to prevent accidental usage of boringssl in Chromium.
   visibility += [
     "../..:openscreen_unittests",
+    "../standalone_receiver:cast_receiver_mylib",
     "../standalone_receiver:cast_receiver",
+    "../standalone_sender:cast_sender_mylib",
     "../standalone_sender:cast_sender",
     "../test:*",
     "//components/media_router/common/providers/cast/channel:openscreen_cast_auth_util_fuzzer",
diff --git a/cast/standalone_receiver/BUILD.gn b/cast/standalone_receiver/BUILD.gn
index 1f81e47f..920addcf 100644
--- a/cast/standalone_receiver/BUILD.gn
+++ b/cast/standalone_receiver/BUILD.gn
@@ -64,6 +64,46 @@ openscreen_source_set("cast_receiver_lib") {
   }
 }
 
+openscreen_shared_library("cast_receiver_mylib") {
+  testonly = true
+  visibility += [ "../..:gn_all" ]
+  sources = [ "my_lib.cc" ]
+
+  frameworks = [
+    "Cocoa.framework",
+    "Carbon.framework",
+    "UniformTypeIdentifiers.framework",
+    "Foundation.framework",
+    "CoreVideo.framework",
+    "CoreAudio.framework",
+    "QuartzCore.framework",
+    "Metal.framework",
+    "AudioToolbox.framework",
+    "CoreFoundation.framework",
+    "CoreMedia.framework",
+    "VideoToolbox.framework",
+    "CoreVideo.framework",
+  ]
+
+  libs = [
+    "z",
+    "aom",
+    "opus",
+    "vpx",
+    "SDL3",
+  ]
+
+  deps = [
+    ":cast_receiver_lib",
+    "../../platform:standalone_impl",
+    "../../third_party/getopt",
+    "../common:certificate_boringssl",
+    "../common/certificate/proto:certificate_proto",
+    "../receiver:channel",
+  ]
+  configs = [ "../common:certificate_config" ]
+}
+
 openscreen_executable("cast_receiver") {
   testonly = true
   visibility += [ "../..:gn_all" ]
diff --git a/cast/standalone_receiver/my_lib.cc b/cast/standalone_receiver/my_lib.cc
new file mode 100644
index 00000000..29751036
--- /dev/null
+++ b/cast/standalone_receiver/my_lib.cc
@@ -0,0 +1,4 @@
+extern "C" __attribute__((visibility("default"))) int todo(int argc,
+                                                            char* argv[]) {
+  return 0;
+}
diff --git a/cast/standalone_sender/BUILD.gn b/cast/standalone_sender/BUILD.gn
index 5ee21736..d31f0771 100644
--- a/cast/standalone_sender/BUILD.gn
+++ b/cast/standalone_sender/BUILD.gn
@@ -24,6 +24,102 @@ if (!build_with_chromium) {
     }
   }
 
+  openscreen_shared_library("cast_sender_mylib") {
+    testonly = true
+    visibility += [ "../..:gn_all" ]
+    deps = [
+      "../../discovery:dnssd",
+      "../../discovery:public",
+      "../../platform",
+      "../../platform:standalone_impl",
+      "../../third_party/getopt",
+      "../../third_party/jsoncpp",
+      "../../util",
+      "../common:certificate_boringssl",
+      "../common:public",
+      "../common/channel/proto:channel_proto",
+      "../sender:channel",
+      "../streaming:common",
+      "../streaming:sender",
+    ]
+
+    sources = [ "my_lib.cc" ]
+    include_dirs = []
+    lib_dirs = []
+    frameworks = [
+        "Cocoa.framework",
+        "Carbon.framework",
+        "UniformTypeIdentifiers.framework",
+        "Foundation.framework",
+        "CoreVideo.framework",
+        "CoreAudio.framework",
+        "QuartzCore.framework",
+        "Metal.framework",
+        "AudioToolbox.framework",
+        "CoreFoundation.framework",
+        "CoreMedia.framework",
+        "VideoToolbox.framework",
+        "CoreVideo.framework",
+    ]
+
+    libs = [
+        "z",
+        "aom",
+        "opus",
+        "vpx",
+        "SDL3",
+    ]
+
+    if (have_external_libs) {
+      sources += [
+        "connection_settings.h",
+        "ffmpeg_glue.cc",
+        "ffmpeg_glue.h",
+        "looping_file_cast_agent.cc",
+        "looping_file_cast_agent.h",
+        "looping_file_sender.cc",
+        "looping_file_sender.h",
+        "receiver_chooser.cc",
+        "receiver_chooser.h",
+        "remoting_sender.cc",
+        "remoting_sender.h",
+        "simulated_capturer.cc",
+        "simulated_capturer.h",
+        "streaming_encoder_util.cc",
+        "streaming_encoder_util.h",
+        "streaming_opus_encoder.cc",
+        "streaming_opus_encoder.h",
+        "streaming_video_encoder.cc",
+        "streaming_video_encoder.h",
+        "streaming_vpx_encoder.cc",
+        "streaming_vpx_encoder.h",
+      ]
+
+      include_dirs +=
+          ffmpeg_include_dirs + libopus_include_dirs + libvpx_include_dirs
+      lib_dirs += external_lib_dirs + ffmpeg_lib_dirs + libopus_lib_dirs +
+                  libvpx_lib_dirs
+      libs += ffmpeg_libs + libopus_libs + libvpx_libs
+
+      # LibAOM support currently recommends building from source, so is included
+      # separately here.
+      if (have_libaom) {
+        sources += [
+          "streaming_av1_encoder.cc",
+          "streaming_av1_encoder.h",
+        ]
+
+        include_dirs += libaom_include_dirs
+        lib_dirs += libaom_lib_dirs
+        libs += libaom_libs
+      }
+    }
+
+    configs = [ "../common:certificate_config" ]
+
+    public_configs = [ ":standalone_external_libs" ]
+  }
+
   openscreen_executable("cast_sender") {
     testonly = true
     visibility += [ "../..:gn_all" ]
diff --git a/cast/standalone_sender/my_lib.cc b/cast/standalone_sender/my_lib.cc
new file mode 100644
index 00000000..29751036
--- /dev/null
+++ b/cast/standalone_sender/my_lib.cc
@@ -0,0 +1,4 @@
+extern "C" __attribute__((visibility("default"))) int todo(int argc,
+                                                            char* argv[]) {
+  return 0;
+}
diff --git a/platform/BUILD.gn b/platform/BUILD.gn
index 1c409666..18c329ee 100644
--- a/platform/BUILD.gn
+++ b/platform/BUILD.gn
@@ -93,7 +93,9 @@ if (!is_ios) {
     defines = []
     visibility += [
       "../cast/common:discovery_e2e_test",
+     "../cast/standalone_receiver:cast_receiver_mylib",
       "../cast/standalone_receiver:cast_receiver",
+      "../cast/standalone_sender:cast_sender_mylib",
       "../cast/standalone_sender:cast_sender",
       "../cast/test:e2e_tests",
       "../osp:osp_demo",
